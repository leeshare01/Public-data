# 修改日志 (Changelog)---## 2026-05-21### 日志和文件命名改为时间戳前缀**改动文件:** `main.py:55`, `run.py:51,141`**内容:**- epoch 日志、CSV、实验摘要、checkpoint 文件名全部加 `MMDD_HHMM` 前缀- 替换全局 `experiment.log` 为每次跑独立的时间戳摘要文件- 旧格式覆盖同名文件 → 新格式每次跑生成独立文件---## 2026-05-20### 修复 worker_init_fn 跨 run 非确定性**改动文件:** `utils/dataloader_utils.py`, 四个 dataloader 文件**内容:** 详见 FIX_LOG.md `[2026-05-20]` 条目。---## 2026-05-19### 1. run.py — 新增 CSV 结果输出**改动:** 每个 epoch 结束后，将指标写入 `exp/{seed}_{dataset}_{label}_{att_loss}_{query_var}_{pe}.csv`。**原因:** 原来只有终端打印和 log 文件，不方便画图分析 SRCC/RL2 随 epoch 的变化趋势。CSV 可用 pandas 一行加载直接画图。**新增列:** `epoch, train_srcc, test_srcc, best_srcc, rl2`### 2. run.py — 新增 Early Stopping**改动:**- `no_improve` 计数器：test SRCC 不创新高时 +1，创新高时归零- `patience` 超限后自动 break 停止训练- 默认 patience=50（可在 config 中配置 `early_stop_patience`）**原因:** FIS-V 仅 400 个训练样本，transformer 架构在 ~500 epoch 即达峰，之后持续过拟合。跑满 1000 epoch 浪费 GPU 时间且无收益。### 3. configs/train_fisv.py — 关闭 dino_loss**改动:** `dino_loss = True` → `dino_loss = False`**原因:** dino_loss 在训练时向预测值注入高斯噪声（std=0.05，见 run.py:103-104）。论文 "Interpretable Long-term Action Quality Assessment" 从未提及此操作。**实验对比:** | 配置 | SRCC ||------|------|| dino_loss=True | 0.679 || 论文报告 (VST) | 0.717 || 差距 | -0.038 |该噪声项作为非必要的强正则化，是性能低于论文的最可能原因。### 4. 已回滚 — transformer.py 残差连接（非 bug）**改动:** `tgt = self_map + self.dropout1(self_map)` → `tgt = tgt + self.dropout1(self_map)` → 已回滚。**原因:** 初次分析认为 self-attention 缺少残差连接是 bug，但实验证明修改后 SRCC 从 0.679 降至 0.412。分析确认这是 TQN 的故意设计——learnable queries（而非 feature-derived tokens）需要在 self-attention 中完全刷新状态，不应保留随机初始化 query 的残差。