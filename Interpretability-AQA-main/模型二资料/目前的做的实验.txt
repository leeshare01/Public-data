模型图和分析相关的是最后几次实验的结果的图，模型有两个数据集，分别是fis_v和rg数据集，目前fis_v数据集不理想。



目前做了七次实验，
第一二次是基线探索，使用的的默认配置，
3-4次出现的问题的非确定性困扰，srcc第3次结果好，为0.679，第4次崩塌
最后三次，

关 dino_loss + 修复确定性（Run 5-7）← 最后三次
 5   │ 05-19 21:57 │ 0.6561 │ 关闭 dino_loss（降低崩塌风险）
6   │ 05-20 12:05 │ 0.6558 │ 修复 seed_worker（闭包→类，可 pickle）
 7   │ 05-20 14:01 │ 0.6558 │ 验证确定性（同配置再跑，完全一致）

Run 5 改进: 关掉 dino_loss 后训练稳定了（代价是 SRCC 从 0.679 降到 0.656）。dino_loss
  的噪声正则化虽然有用，但在数据不确定的情况下是双刃剑。

  Run 6 改进: 修复 seed_worker，_SeedWorker 类替代闭包。这是工程可靠性改进而非性能改进——SRCC 没变，但从此同配置同结果。

  Run 7 改进: 零改动，纯粹验证。Run 6=Run 7 完完全全一致（小数点后 16 位都一样），证明了确定性修复成功。

现在存在的问题：
                问题            │ 严重程度 │              现象               │
 Fis-V RL2 从未收敛     │ 高          │ 全程 2.0~3.6 振荡，MSE 优化不动
Fis-V SRCC 距论文差 0.025 │ 中       │ 0.693 vs BMVC 0.717             │
RG 缺 early_stop_patience │ 中       │ epoch 58 后白跑 942 个 epoch    │
IJCV 模块缺失             │ 中       │ D/Q 分解、Uncertainty 未实现    │
 I3D 权重不可用            │ 低       │ 路径指向 Linux 服务器           │